# Frontend

Run the example frontend with:

```bash
uv run streamlit run apps/legal_doc_qa/frontend/app.py
```

The frontend expects the example backend to be running at `http://localhost:8010` by default.
